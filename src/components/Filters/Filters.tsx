import React from 'react';

import './filters.scss';

const Filters = () => {
    return (
        <div className="app-filters">
            <div className="filters-category">category</div>
            <div className="filters-search">search</div>
            <div className="filters-sort">filter</div>
        </div>
    );
};

export default Filters;
